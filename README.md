# Spotify-genre-segmentation
Unsupervised machine learning pipeline that clusters ~32,000 Spotify tracks by audio features using K-Means clustering and PCA dimensionality reduction. Evaluates models using Elbow and Silhouette analysis, and constructs a content-based song recommendation engine utilizing Euclidean distance similarity within the optimal clusters.
